---
name: ZZKK PRD
description: 从基础信息表先补充“用户需求描述/验收标准”副本，再生成客户/产品需求说明书及评审表 6 份交付物。适用于用户提供《客户需求说明书》《产品需求说明书》《评审表》模板，并要求主动询问模块编号、模块名称、初稿版本号、终稿版本号、模板角色映射和覆盖策略的场景。
argument-hint: "[基础信息表路径]"
disable-model-invocation: true
user-invocable: true
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
  - AskUserQuestion
model: sonnet
---

# ZZKK PRD

## 目标

把 `基础信息表.xlsx` 变成 6 份交付物：

- `<模块名称>-客户需求说明书_<初稿版本号>.docx`
- `<模块名称>-客户需求说明书_<终稿版本号>.docx`
- `<模块名称>-客户需求说明书_同级评审会议记录表.xlsx`
- `<模块名称>-模块级产品需求分析说明书_<初稿版本号>.docx`
- `<模块名称>-模块级产品需求分析说明书_<终稿版本号>.docx`
- `<模块名称>-模块级产品需求分析说明书_同级评审会议记录表.xlsx`

## 严格约束

1. **必须主动询问**：
   - 模块编号，例如 `904`
   - 模块名称，例如 `桌面环境`
   - 初稿版本号，例如 `0.1`
   - 终稿版本号，例如 `1.0`
2. **模板角色不能靠文件名猜**：即使用户给的 3 个模板文件名接近标准名，也要主动确认：
   - 哪个是客户需求说明书模板
   - 哪个是产品需求分析说明书模板
   - 哪个是评审表模板
3. **每次都要主动询问是否覆盖现有文件**，默认不覆盖。
4. **必须严格继承模板格式**：模板中加粗的字段标签，如“功能描述：”“关联模块描述：”“验收标准：”，在新生成文件中也必须保持同样的加粗样式。
5. 当前客户需求说明书支持简化版 DOCX 模板：生成时只补充“应用场景表”和“用户需求描述表”这两个表格，不再依赖旧版客户模板中的概述、模块分解、交付物、术语、参考资料等区块。
6. 评审表只有 1 个通用模板时，需要基于该模板复制生成客户评审表和产品评审表两份。
7. **必须先提醒用户**：用户提供给 skill 的就是原始基础信息表，且这份原始表中必须具备 `用户需求id` 和 `研发需求id` 两列；执行时还会继续校验生成所需的 `研发需求名称`、`研发需求描述` 列。
8. **必须基于用户提供的原始基础信息表做补充和完善**：先补充 `用户需求描述`、完善 `验收标准`，另存为一份新的基础信息表副本，再基于这份副本生成 6 个交付物。

## 执行步骤

### 第一步：收集必要输入

按下面顺序主动向用户确认：

1. 基础信息表路径
   - 若用户未提供，优先尝试当前工作目录中的 `基础信息表.xlsx`
   - 明确告诉用户：这份用户提供的文件会被当作原始基础信息表
   - 先提醒用户：原始基础信息表必须包含 `用户需求id` 与 `研发需求id`
   - 然后再校验是否也具备生成所需的 `研发需求名称`、`研发需求描述`
2. 模块编号
3. 模块名称
4. 初稿版本号
5. 终稿版本号
6. 3 个模板文件分别对应什么角色
7. 输出目录
8. 是否覆盖现有文件（默认不覆盖）

### 第二步：先做 dry-run 计划

在真正写文件前，先运行：

```bash
python3 "~/.claude/skills/ZZKK-PRD/skill.py" plan \
  --workbook "<基础信息表路径>" \
  --customer-template "<客户模板路径>" \
  --product-template "<产品模板路径>" \
  --review-template "<评审表模板路径>" \
  --module-code "<模块编号>" \
  --module-name "<模块名称>" \
  --draft-version "<初稿版本号>" \
  --final-version "<终稿版本号>" \
  --output-dir "<输出目录>"
```

检查 dry-run 输出：

- 原始基础信息表缺失必需列时，是否明确报错提示用户补齐
- 计划另存的“已补充用户需求描述和验收标准”基础信息表副本路径是否正确
- 6 个目标文件名是否正确
- 评审表命名是否正确绑定到被评审说明书
- 记录编号 / 文档编号是否包含模块编号

### 第三步：执行生成

执行时必须先对原始基础信息表做这些处理：

- 校验原始表是否包含 `用户需求id`、`研发需求id`、`研发需求名称`、`研发需求描述`
- 若缺少 `用户需求描述` 列，则自动插入并补充内容
- 若缺少 `验收标准` 列，则自动插入并补充内容
- 若 `验收标准` 已存在，则按当前内容做规范化和完善
- 将补充后的结果另存为一份新的基础信息表副本
- 再基于这份副本生成 6 个交付物

只有在用户明确同意后才执行：

```bash
python3 "~/.claude/skills/ZZKK-PRD/skill.py" run \
  --workbook "<基础信息表路径>" \
  --customer-template "<客户模板路径>" \
  --product-template "<产品模板路径>" \
  --review-template "<评审表模板路径>" \
  --module-code "<模块编号>" \
  --module-name "<模块名称>" \
  --draft-version "<初稿版本号>" \
  --final-version "<终稿版本号>" \
  --output-dir "<输出目录>" \
  --overwrite
```

如果用户没有明确允许覆盖，就**不要**传 `--overwrite`。

### 第四步：执行后校验

运行：

```bash
python3 "~/.claude/skills/ZZKK-PRD/skill.py" verify \
  --workbook "<基础信息表路径>" \
  --customer-template "<客户模板路径>" \
  --product-template "<产品模板路径>" \
  --review-template "<评审表模板路径>" \
  --module-code "<模块编号>" \
  --module-name "<模块名称>" \
  --draft-version "<初稿版本号>" \
  --final-version "<终稿版本号>" \
  --output-dir "<输出目录>"
```

然后再做一次人工抽查：

- 新的基础信息表副本已生成，且路径与 dry-run 一致
- 6 个文件都存在
- 文件名符合模块名称与版本号规则
- DOCX 内文档编号包含模块编号
- XLSX 内记录编号包含模块编号
- 模板中要求加粗的字段标签没有丢样式

## 输出汇报格式

执行结束后，简明汇报：

1. 使用的输入参数
2. 生成的 6 个文件路径
3. 是否覆盖了旧文件
4. dry-run / run / verify 是否成功
5. 若有未自动处理的模板差异，明确指出具体文件和原因
