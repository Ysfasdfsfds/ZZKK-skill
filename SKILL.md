---
name: ZZKK PRD
version: "1.03"
description: 从基础信息表先补充“用户需求描述/验收标准”副本，再生成客户/产品需求说明书及评审表 6 份交付物。适用于用户提供《客户需求说明书》《产品需求说明书》《评审表》模板，并要求主动询问模块编号、模块名称、初稿版本号、终稿版本号、模板角色映射和覆盖策略的场景。
argument-hint: "[基础信息表路径]"
disable-model-invocation: true
user-invocable: true
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
  - AskUserQuestion
model: sonnet
---

# ZZKK PRD

## 目标

把 `基础信息表.xlsx` 变成 6 份交付物：

- `<模块名称>-客户需求说明书_<初稿版本号>.docx`
- `<模块名称>-客户需求说明书_<终稿版本号>.docx`
- `<模块名称>-客户需求说明书_同级评审会议记录表.xlsx`
- `<模块名称>-模块级产品需求分析说明书_<初稿版本号>.docx`
- `<模块名称>-模块级产品需求分析说明书_<终稿版本号>.docx`
- `<模块名称>-模块级产品需求分析说明书_同级评审会议记录表.xlsx`

## 严格约束

1. **必须主动询问**：
   - 模块编号，例如 `904`
   - 模块名称，例如 `桌面环境`
   - 产线名称英文缩写，例如 `DT`
   - 产品名称，例如 `V11 SP1`
   - 初稿版本号，例如 `0.1`
   - 终稿版本号，例如 `1.0`
   - 产品需求说明书初稿撰写时间（按用户输入原样写入，不限制格式）
   - 产品需求说明书评审表时间（按用户输入原样写入，不限制格式）
   - 产品需求说明书终稿撰写时间（按用户输入原样写入，不限制格式）
   - 负责该模块的模块产品经理姓名（写入《评审记录表》的主持人）
   - 负责该模块的项目经理（写入《评审记录表》的书记员）
   - 评审员名单，必须提醒用户应包括研发负责人、项目经理、产品级产品经理、安全负责人、测试负责人
   - 研发负责人姓名（写入评审问题“提出人”）
   - 产品级产品经理姓名（写入评审问题“提出人”）
   - 安全负责人姓名（写入评审问题“提出人”）
   - 测试负责人姓名（写入评审问题“提出人”）
   - 对应 QA（写入《评审记录表》的其他人员）
   - 上述任一字段缺失时，必须主动追问用户，不得猜测或静默使用默认值
2. **模板角色不能靠文件名猜**：即使用户给的 3 个模板文件名接近标准名，也要主动确认：
   - 哪个是客户需求说明书模板
   - 哪个是产品需求分析说明书模板
   - 哪个是评审表模板
3. **每次都要主动询问是否覆盖现有文件**，默认不覆盖。
4. **必须严格继承模板格式**：模板中加粗的字段标签，如“功能描述：”“关联模块描述：”“验收标准：”，在新生成文件中也必须保持同样的加粗样式。
5. **文件编号必须以模板要求为准**：
   - 客户需求说明书模板要求：`产线名称英文缩写-模块ID-CRS-版本号`，例如 `DT-302-CRS-V0.1`
   - 产品需求分析说明书模板要求：`产线名称英文缩写-产品名称-模块ID-PRD-版本号`，例如 `DT-V11 SP1-302-PRD-V0.1`
   - 不得把产品名称写入客户需求说明书编号，除非用户提供的客户模板本身明确要求产品名称
6. 当前客户需求说明书支持简化版 DOCX 模板：生成时只补充“应用场景表”和“用户需求描述表”这两个表格，不再依赖旧版客户模板中的概述、模块分解、交付物、术语、参考资料等区块。
7. 评审表只有 1 个通用模板时，需要基于该模板复制生成客户评审表和产品评审表两份。
8. **必须先提醒用户**：用户提供给 skill 的就是原始基础信息表，且这份原始表中必须具备 `用户需求id` 和 `研发需求id` 两列；执行时还会继续校验生成所需的 `研发需求名称`、`研发需求描述` 列。
9. **必须基于用户提供的原始基础信息表做补充和完善**：先补充 `用户需求描述`、完善 `验收标准`，另存为一份新的基础信息表副本，再基于这份副本生成 6 个交付物。
10. **初稿、评审表、终稿必须形成流程闭环**：
   - 生成顺序必须是：先生成初稿，再基于初稿生成评审表，最后根据评审表形成终稿。
   - 终稿不是与初稿并列生成的独立版本，而是“初稿经过评审表记录后形成的终稿”。
   - 评审表的“工作产品/标识”必须指向对应说明书的初稿文件，而不是终稿文件。
   - 终稿必须基于同一份“已补充用户需求描述和验收标准”的基础信息表副本生成。
   - 同一类说明书的初稿与终稿必须覆盖同一批需求 ID；不得出现初稿有、终稿缺，或终稿新增了未评审需求的情况。
   - 终稿可更新文档编号、版本号、日期和评审后完善表述，但不得改变需求范围。
   - 终稿版本记录必须体现“根据同级评审记录由初稿形成终稿”，并包含对应评审记录编号。
11. **评审表必须做语义评审，不能写机械占位结论**：
   - 不允许默认写“无需整改”“无需处理”“无待解决问题”来代替评审。
   - 客户需求说明书评审必须站在模块产品经理、产品级产品经理、安全负责人、测试负责人等角度，检查应用场景是否泛化、用户角色/业务价值是否清楚、安全/性能/异常提示等非功能约束是否闭合。
   - 产品需求分析说明书评审必须站在项目经理、研发负责人、安全负责人、测试负责人等角度，检查功能详述是否具体、关联模块/接口契约是否明确、异常流程/边界条件/测试验收口径是否可落地。
   - 评审表中应写入具体问题、解决办法、具体提出人姓名和问题状态；若问题已在终稿中解决，状态可为“关闭”，但要说明“对应终稿已修订闭环”。
   - 评审问题“提出人”不得填写负责该模块的模块产品经理；模块产品经理可以作为主持人或跟踪责任人，但具体问题提出人应来自产品级产品经理、研发负责人、安全负责人、测试负责人等评审角色。
   - 终稿必须根据评审表中的意见修订内容，例如补充“背景/使用目标/成功标准”、用户价值、关联模块、输入/过程/输出、异常流程、质量特性、参考资料和评审记录追踪。
   - 评审表可以提到“对应终稿已修订闭环”，但“工作产品/标识”和问题记录中的文档号仍必须指向初稿，避免把评审对象误写成终稿。
12. **文档首页文件编号必须清理模板占位**：
   - 若首页模板已存在“文件编号：……”及相邻占位内容，生成时必须只保留最终的 `文件编号：<实际编号>`。
   - 不得出现最终编号与模板示例编号并存，例如 `文件编号：KOS11-302-CRS-V0.1` 后仍保留 `DT-模块ID-CRS-版本号`。
13. **“无/不涉及”类占位必须统一写为“无”**：
   - 所有 DOCX/XLSX 生成内容中，若字段含义是无、暂无、不涉及、无父模块、无特殊说明、无协作模块等，必须统一输出 `无`。
   - 不得使用 `/` 作为“无”的占位文本；执行后校验时也要检查文档中是否仍存在单独成值的 `/`。
14. **产品需求说明书日期必须按用户输入原样写入**：
   - 不强制日期格式；用户输入 `2026.1.13`、`2026-01-13`、`1月13日` 等都按原样写入。
   - 产品需求说明书初稿：首页表格“拟制”行的“日期”写初稿撰写时间；版本说明表只写初稿记录，日期为初稿撰写时间。
   - 产品需求说明书终稿：首页表格“拟制/审核/批准”三行日期分别写初稿撰写时间、评审表时间、终稿撰写时间；版本说明表写初稿和终稿两条记录，日期分别为初稿撰写时间和终稿撰写时间。
   - 产品需求说明书评审表：`项目名称` 和 `工作产品` 写 `银河麒麟桌面操作系统<产品名称>`（若用户输入已包含“银河麒麟”则直接使用）；`工作产品评审规模` 写对应产品需求说明书初稿文件名；`项目阶段` 写 `设计阶段`；`会议日期/会议时间` 写用户提供的评审表时间。

## 执行步骤

### 第一步：收集必要输入

按下面顺序主动向用户确认：

1. 基础信息表路径
   - 若用户未提供，优先尝试当前工作目录中的 `基础信息表.xlsx`
   - 明确告诉用户：这份用户提供的文件会被当作原始基础信息表
   - 先提醒用户：原始基础信息表必须包含 `用户需求id` 与 `研发需求id`
   - 然后再校验是否也具备生成所需的 `研发需求名称`、`研发需求描述`
2. 模块编号
3. 模块名称
4. 产线名称英文缩写
5. 产品名称
6. 初稿版本号
7. 终稿版本号
8. 产品需求说明书初稿撰写时间（不限制格式，按用户输入原样写入）
9. 产品需求说明书评审表时间（不限制格式，按用户输入原样写入）
10. 产品需求说明书终稿撰写时间（不限制格式，按用户输入原样写入）
11. 3 个模板文件分别对应什么角色
12. 评审记录表人员信息：
   - 负责该模块的模块产品经理姓名是？这个对应主持人
   - 负责该模块的项目经理是？这个对应书记员
   - 评审员都是谁？应该包括研发负责人、项目经理、产品级产品经理、安全负责人、测试负责人
   - 研发负责人是谁？这个写入产品需求分析说明书评审问题“提出人”
   - 产品级产品经理是谁？这个写入客户需求说明书评审问题“提出人”
   - 安全负责人是谁？这个写入评审问题“提出人”
   - 测试负责人是谁？这个写入评审问题“提出人”
   - 对应的 QA 是？这个对应其他人员
13. 输出目录
14. 是否覆盖现有文件（默认不覆盖）

### 第二步：先做 dry-run 计划

在真正写文件前，先运行：

```bash
python3 "~/.claude/skills/ZZKK-PRD/skill.py" plan \
  --workbook "<基础信息表路径>" \
  --customer-template "<客户模板路径>" \
  --product-template "<产品模板路径>" \
  --review-template "<评审表模板路径>" \
  --module-code "<模块编号>" \
  --module-name "<模块名称>" \
  --product-line-code "<产线名称英文缩写>" \
  --product-name "<产品名称>" \
  --draft-version "<初稿版本号>" \
  --final-version "<终稿版本号>" \
  --product-draft-date "<产品需求说明书初稿撰写时间>" \
  --product-review-date "<产品需求说明书评审表时间>" \
  --product-final-date "<产品需求说明书终稿撰写时间>" \
  --module-product-manager "<模块产品经理>" \
  --module-project-manager "<模块项目经理>" \
  --reviewers "<评审员名单>" \
  --rd-owner "<研发负责人>" \
  --product-owner "<产品级产品经理>" \
  --test-owner "<测试负责人>" \
  --security-owner "<安全负责人>" \
  --qa "<对应QA>" \
  --output-dir "<输出目录>"
```

检查 dry-run 输出：

- 原始基础信息表缺失必需列时，是否明确报错提示用户补齐
- 计划另存的“已补充用户需求描述和验收标准”基础信息表副本路径是否正确
- 6 个目标文件名是否正确
- 评审表命名是否正确绑定到被评审说明书
- 评审表“工作产品/标识”是否绑定对应说明书初稿
- 初稿与终稿是否会基于同一份已补充基础信息表副本生成
- 生成流程是否为“初稿 -> 评审表 -> 终稿”
- 评审表是否会生成角色化、具体的问题和解决办法，而不是默认“无需整改”
- 记录编号 / 文档编号是否符合模板规则：客户说明书为 `产线名称英文缩写-模块ID-CRS-版本号`，产品说明书为 `产线名称英文缩写-产品名称-模块ID-PRD-版本号`
- 产品需求说明书初稿、评审表、终稿日期是否会按用户输入原样写入对应位置

### 第三步：执行生成

执行时必须先对原始基础信息表做这些处理：

- 校验原始表是否包含 `用户需求id`、`研发需求id`、`研发需求名称`、`研发需求描述`
- 若缺少 `用户需求描述` 列，则自动插入并补充内容
- 若缺少 `验收标准` 列，则自动插入并补充内容
- 若 `验收标准` 已存在，则按当前内容做规范化和完善
- 将补充后的结果另存为一份新的基础信息表副本
- 再基于这份副本生成 6 个交付物

只有在用户明确同意后才执行：

```bash
python3 "~/.claude/skills/ZZKK-PRD/skill.py" run \
  --workbook "<基础信息表路径>" \
  --customer-template "<客户模板路径>" \
  --product-template "<产品模板路径>" \
  --review-template "<评审表模板路径>" \
  --module-code "<模块编号>" \
  --module-name "<模块名称>" \
  --product-line-code "<产线名称英文缩写>" \
  --product-name "<产品名称>" \
  --draft-version "<初稿版本号>" \
  --final-version "<终稿版本号>" \
  --product-draft-date "<产品需求说明书初稿撰写时间>" \
  --product-review-date "<产品需求说明书评审表时间>" \
  --product-final-date "<产品需求说明书终稿撰写时间>" \
  --module-product-manager "<模块产品经理>" \
  --module-project-manager "<模块项目经理>" \
  --reviewers "<评审员名单>" \
  --rd-owner "<研发负责人>" \
  --product-owner "<产品级产品经理>" \
  --test-owner "<测试负责人>" \
  --security-owner "<安全负责人>" \
  --qa "<对应QA>" \
  --output-dir "<输出目录>" \
  --overwrite
```

如果用户没有明确允许覆盖，就**不要**传 `--overwrite`。

### 第四步：执行后校验

运行：

```bash
python3 "~/.claude/skills/ZZKK-PRD/skill.py" verify \
  --workbook "<基础信息表路径>" \
  --customer-template "<客户模板路径>" \
  --product-template "<产品模板路径>" \
  --review-template "<评审表模板路径>" \
  --module-code "<模块编号>" \
  --module-name "<模块名称>" \
  --product-line-code "<产线名称英文缩写>" \
  --product-name "<产品名称>" \
  --draft-version "<初稿版本号>" \
  --final-version "<终稿版本号>" \
  --product-draft-date "<产品需求说明书初稿撰写时间>" \
  --product-review-date "<产品需求说明书评审表时间>" \
  --product-final-date "<产品需求说明书终稿撰写时间>" \
  --module-product-manager "<模块产品经理>" \
  --module-project-manager "<模块项目经理>" \
  --reviewers "<评审员名单>" \
  --rd-owner "<研发负责人>" \
  --product-owner "<产品级产品经理>" \
  --test-owner "<测试负责人>" \
  --security-owner "<安全负责人>" \
  --qa "<对应QA>" \
  --output-dir "<输出目录>"
```

然后再做一次人工抽查：

- 新的基础信息表副本已生成，且路径与 dry-run 一致
- 6 个文件都存在
- 文件名符合模块名称与版本号规则
- DOCX 内文档编号包含模块编号
- XLSX 内记录编号包含模块编号
- XLSX 评审表“工作产品/标识”指向对应初稿文件，不能误指终稿文件
- 产品需求说明书评审表的项目名称和工作产品为 `银河麒麟桌面操作系统<产品名称>`，工作产品评审规模为对应初稿文件名，项目阶段为 `设计阶段`，会议日期/会议时间为用户提供的评审表时间
- 产品需求说明书初稿首页和版本说明表只写初稿撰写时间；终稿首页写初稿/评审/终稿三个时间，版本说明表写初稿和终稿时间
- XLSX 评审表不应含默认占位式“无需整改”“无需处理”“无待解决问题”
- XLSX 评审表应包含角色化问题描述、解决办法、具体提出人姓名和关闭状态
- 同一类说明书的初稿与终稿包含同一批需求 ID
- 终稿版本记录包含对应评审记录编号，体现由初稿经评审形成终稿
- 终稿内容应体现评审意见已被吸收，例如客户终稿补充场景差异和成功标准，产品终稿补充关联模块、处理过程、异常流程、质量特性与参考资料
- 模板中要求加粗的字段标签没有丢样式
- DOCX/XLSX 中表示“无/不涉及”的内容统一为“无”，不应存在单独成值的 `/`

## 输出汇报格式

执行结束后，简明汇报：

1. 使用的输入参数
2. 生成的 6 个文件路径
3. 是否覆盖了旧文件
4. dry-run / run / verify 是否成功
5. 若有未自动处理的模板差异，明确指出具体文件和原因
